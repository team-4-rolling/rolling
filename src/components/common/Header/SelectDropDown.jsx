import React from "react";

//
export const options = ["가족", "지인", "연인"];

function SelectDropDown({ options }) {
  return (
    <select>
      <div></div>
    </select>
  );
}
export default SelectDropDown;
